<% if @posixaccount %>
  <div class="modal-header">
    <button type="button" class="close" data-dismiss="modal">×</button>
    <h3>Confirmation</h3>
  </div>
  <div class="modal-body">
    <p>Please confirm that you would like to enable <em><%= @posixaccount.uid %></em>. This event will be logged, and the password will need to be set manually.</p>
  </div>
  <div class="modal-footer">
    <form id="enable-posixaccount" method="post" action="/ldap/posixaccount/<%= @posixaccount.uid %>/enable">
      <input class="btn btn-success" type="submit" value="Enable">
      <a href="#" class="btn" data-dismiss="modal">Cancel</a>
    </form>
  </div>
<% end %>
